# 🧪 YT-MP3 系統功能與整合測試報告

**測試時間**：2026-05-07  
**測試版本**：v0.6.0 (包含「搜尋影片」與「網址下載」新功能)  
**測試涵蓋範圍**：UI 介面、核心 API 路由、 yt-dlp 解析引擎、狀態儲存 (Store)  

---

## 1. 核心探索功能測試 (Search & Explore)

### 測試個案 1-1：搜尋影片功能
* **測試步驟**：
  1. 在左側導覽列點擊「🔍 搜尋影片」。
  2. 於搜尋列輸入 `財報` (或 `2330`)，按下 Enter 或點擊「搜尋」。
* **預期結果**：
  * 畫面顯示「搜尋中...」狀態。
  * 後端呼叫 `_sync_search_videos_yt_dlp`，不耗用 YouTube API Quota。
  * 畫面呈現網格 (Grid) 佈局，展示約 40-50 筆相關影片。
  * **長度過濾器豁免**：即使影片超過 60 分鐘或短於 3 分鐘 (Shorts)，依然會正常顯示。
* **測試狀態**：✅ **PASS** (API HTTP 200，成功回傳 40 筆資料)
* **畫面截圖**：
![Search Videos](C:/Users/Danny/.gemini/antigravity/brain/2a10910e-e416-439c-a7cf-b43c802f7892/search_videos_png_1778131764952.png)

### 測試個案 1-2：發燒影片與單一頻道 (長度過濾豁免驗證)
* **測試步驟**：
  1. 切換至「🔥 發燒影片」或「單一頻道」。
  2. 觀察列表中的影片長度。
* **預期結果**：
  * 長度過濾 (`min_duration_minutes`, `max_duration_minutes`) 已停用 (`apply_duration_filter=False`)。
  * 清單中能正常顯示小於 1 分鐘的 Shorts，以及大於 1 小時的直播存檔。
* **測試狀態**：✅ **PASS**

---

## 2. 網址下載功能測試 (URL Download)

### 測試個案 2-1：單一影片網址解析
* **測試步驟**：
  1. 導覽至「🔗 網址下載」。
  2. 輸入 `https://www.youtube.com/watch?v=dQw4w9WgXcQ`，點擊「解析網址」。
* **預期結果**：
  * 畫面出現該單一影片的縮圖、標題 (`Rick Astley - Never Gonna Give You Up`) 與時間長度。
  * **自動勾選機制**：因為是單一影片，系統會自動將其勾選，右側的「下載佇列」會同步增加該筆項目。
* **測試狀態**：✅ **PASS**
* **畫面截圖**：
![URL Download](C:/Users/Danny/.gemini/antigravity/brain/2a10910e-e416-439c-a7cf-b43c802f7892/url_download_png_1778131792088.png)

### 測試個案 2-2：播放清單 (Playlist) 網址解析
* **測試步驟**：
  1. 於網址下載頁面，輸入任一合法的 YouTube Playlist 網址。
* **預期結果**：
  * 後端透過 `yt-dlp` 的 `extract_flat=True` 快速解析清單中所有影片。
  * 畫面渲染出所有清單內影片。
  * 上方出現「✅ 全選」與「🟩 全不選」按鈕。
  * 點擊「全選」後，所有影片順暢加入右側下載佇列。
* **測試狀態**：✅ **PASS**

---

## 3. 設定與限制測試 (Settings)

### 測試個案 3-1：最新影片長度限制 UI
* **測試步驟**：
  1. 進入「設定」頁面 (`/settings`)。
* **預期結果**：
  * 相關欄位已正確更名為「**最新影片**最短長度 (分鐘)」與「**最新影片**最長長度 (分鐘)」。
  * 輸入數值並儲存後，切換回「最新影片」，只有符合該區間的影片會被顯示。
* **測試狀態**：✅ **PASS**

---

## 4. 自動化回歸測試狀態 (Regression Tests)

本次更新涉及重大架構變更（包含多帳號支援與移除 RSS 依賴），已同步修復所有後端測試套件。

| 測試模組 | 測試項目數 | 狀態 | 說明 |
| :--- | :---: | :---: | :--- |
| `test_download.py` | 8 | ✅ PASS | MP3/MP4 下載機制與進度追蹤 |
| `test_latest_videos.py` | 7 | ✅ PASS | 最新影片抓取與時長過濾驗證 |
| `test_normalize.py` | 27 | ✅ PASS | MP3Gain 音量正規化與檔名安全機制 |
| `test_settings.py` | 29 | ✅ PASS | 設定檔讀寫與邊界值驗證 |
| `test_subscriptions.py` | 4 | ✅ PASS | 多帳號訂閱清單獲取與分頁處理 |
| **總計** | **75** | ✅ **100% PASS** | 核心邏輯無任何損壞 |
